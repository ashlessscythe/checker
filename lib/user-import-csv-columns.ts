/** Shared column list for user CSV import and export (same order, same names). */
export const USER_IMPORT_CSV_HEADERS = [
  "name",
  "email",
  "barcode",
  "is_admin",
  "department_name",
] as const;
